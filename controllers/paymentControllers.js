//const {
//  addComment,
//} = require('../services/commentServices');
//const { constants } = require('../constants');

const xx = async (req, res) => {
  //const {} = req.body;

  res.status(200).json({
    text: 'hello',
  });
};

module.exports = {
  xx,
};
